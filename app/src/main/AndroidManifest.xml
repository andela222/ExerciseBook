<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.exercisebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_gym"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddEditMeasurementsActivity"></activity>
        <activity android:name=".WorkoutActivity" />
        <activity android:name=".AddEditExercisesActivity" />
        <activity
            android:name=".DisplayTabActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddEditUserActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PickDate"
            android:icon="@drawable/ic_calendar" />
    </application>

</manifest>